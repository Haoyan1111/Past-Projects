Following steps help you run the project
1. executed the setup.sh file
    type command into terminal:
        $ chmod +x setup.sh
        $ ./setup.sh
2. install mongoDB and docker 
3. run command $ docker compose up --build --force-recreate